{% extends 'base.html.twig' %}

{% block title %}Delivery{% endblock %}

{% block body %}
    <h1>Delivery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ delivery.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ delivery.city }}</td>
            </tr>
            <tr>
                <th>Meeting</th>
                <td>{{ delivery.meeting }}</td>
            </tr>
            <tr>
                <th>CheckinTime</th>
                <td>{{ delivery.checkinTime ? delivery.checkinTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('delivery_index') }}">back to list</a>

    <a href="{{ path('delivery_edit', {'id': delivery.id}) }}">edit</a>

    {{ include('delivery/_delete_form.html.twig') }}
{% endblock %}
