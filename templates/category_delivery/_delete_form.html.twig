<form method="post" action="{{ path('category_delivery_delete', {'id': category_delivery.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_delivery.id) }}">
    <button class="btn">Delete</button>
</form>
