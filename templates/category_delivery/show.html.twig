{% extends 'base.html.twig' %}

{% block title %}CategoryDelivery{% endblock %}

{% block body %}
    <h1>CategoryDelivery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_delivery.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ category_delivery.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_delivery_index') }}">back to list</a>

    <a href="{{ path('category_delivery_edit', {'id': category_delivery.id}) }}">edit</a>

    {{ include('category_delivery/_delete_form.html.twig') }}
{% endblock %}
