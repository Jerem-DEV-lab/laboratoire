{% extends 'base.html.twig' %}

{% block title %}CategoryDelivery index{% endblock %}

{% block body %}
    <h1>CategoryDelivery index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category_delivery in category_deliveries %}
            <tr>
                <td>{{ category_delivery.id }}</td>
                <td>{{ category_delivery.title }}</td>
                <td>
                    <a href="{{ path('category_delivery_show', {'id': category_delivery.id}) }}">show</a>
                    <a href="{{ path('category_delivery_edit', {'id': category_delivery.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('category_delivery_new') }}">Create new</a>
{% endblock %}
